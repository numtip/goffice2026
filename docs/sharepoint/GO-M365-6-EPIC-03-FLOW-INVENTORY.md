# GO-M365-6 — EPIC-03 Flow Inventory

> **Date**: 2026-07-27  
> **Flow**: GO Metric Approval Workflow  
> **Flow ID**: `40e04977-38cf-42ad-a1e5-bbefbf5cbac1`

---

## Flow Action Inventory

### 1. Trigger: When Power Apps calls a flow (V2)

| Property | Value |
|---|---|
| **Type** | Request |
| **Kind** | PowerAppV2 |
| **Connector** | Power Apps |
| **Authentication** | OAuth (researchmju@mju.ac.th) |

#### Input Schema

| Key | Title | Type | Required |
|---|---|---|---|
| `number` | SharePointItemId | Number | ✅ |
| `text` | MetricName | String | ✅ |
| `text_1` | MetricValue | String | ✅ |
| `text_2` | SubmitterEmail | String | ✅ |
| `text_3` | SubmitterName | String | ✅ |
| `text_4` | ApproverEmail | String | ✅ |
| `text_5` | ApproverName | String | ✅ |

> **Note**: Property keys (`text`, `text_1`, etc.) are auto-generated by Power Automate. The `title` field is what callers use to identify parameters.

---

### 2. Initialize variable

| Property | Value |
|---|---|
| **Type** | InitializeVariable |
| **Connector** | Variable |
| **Name** | ApprovalStatus |
| **Type** | String |
| **Value** | "Pending" |

---

### 3. Get items

| Property | Value |
|---|---|
| **Type** | GetItems |
| **Connector** | SharePoint |
| **Site** | https://maejo365.sharepoint.com/sites/msteams_54adc4 |
| **List** | GO Approval Workflow |
| **Filter Query** | `Id eq @{triggerBody()?['number']}` |
| **Connection** | researchmju@mju.ac.th |

---

### 4. For each (outer)

| Property | Value |
|---|---|
| **Type** | Foreach |
| **Scope** | Wraps Start and wait for an approval, inner For each 1 |

---

### 5. Start and wait for an approval

| Property | Value |
|---|---|
| **Type** | Approval |
| **Connector** | Standard approvals |
| **Approval Type** | Approve/Reject — First to respond |
| **Title** | Green Office metric approval — {MetricName} |
| **Assigned To** | {ApproverEmail} |
| **Details** | Metric Name, Metric Value (from trigger inputs) |
| **Enable Notifications** | Yes |
| **Enable Reassignment** | Yes |
| **Connection** | Standard approvals |

---

### 6. For each 1 (inner)

| Property | Value |
|---|---|
| **Type** | Foreach |
| **Scope** | Auto-wrapped by Power Automate for Responses array |
| **Notes** | Iterates over approval responses (single response for First to respond) |

---

### 7. Condition

| Property | Value |
|---|---|
| **Type** | If |
| **Operator** | And (default) |
| **Row 1 Left** | Responses Approver response |
| **Row 1 Operator** | is equal to |
| **Row 1 Right** | (to be configured — should be "Approve") |

**Note**: Configuration may have been partially lost during flow edits. Needs re-verification.

---

### 8. Update item (IF TRUE) — NOT IMPLEMENTED

| Property | Value |
|---|---|
| **Type** | UpdateItem |
| **Connector** | SharePoint |
| **Site** | https://maejo365.sharepoint.com/sites/msteams_54adc4 |
| **List** | GO Approval Workflow |
| **Fields to set** | ApprovalStatus = "Approved", ApprovalDate = utcNow(), ApproverComments = responses comments, FlowRunId = workflow() |
| **Status** | ❌ Not implemented |

---

### 9. Update item (IF FALSE) — NOT IMPLEMENTED

| Property | Value |
|---|---|
| **Type** | UpdateItem |
| **Connector** | SharePoint |
| **Site** | https://maejo365.sharepoint.com/sites/msteams_54adc4 |
| **List** | GO Approval Workflow |
| **Fields to set** | ApprovalStatus = "Rejected", ApprovalDate = utcNow(), ApproverComments = responses comments, FlowRunId = workflow() |
| **Status** | ❌ Not implemented |

---

### 10. Compose

| Property | Value |
|---|---|
| **Type** | Compose |
| **Connector** | Data Operation |
| **Status** | Pre-existing (not yet modified for structured result) |

---

## SharePoint Field Mapping

### On Approved

| SharePoint Field | Value Source |
|---|---|
| ApprovalStatus | Literal "Approved" |
| ApprovalDate | `utcNow()` |
| ApproverComments | `responses('Start_and_wait_for_an_approval')?['comments']` |
| FlowRunId | `workflow()?['run']?['name']` |

### On Rejected

| SharePoint Field | Value Source |
|---|---|
| ApprovalStatus | Literal "Rejected" |
| ApprovalDate | `utcNow()` |
| ApproverComments | `responses('Start_and_wait_for_an_approval')?['comments']` |
| FlowRunId | `workflow()?['run']?['name']` |

### Unchanged Fields

All other SharePoint fields (Title, ActorRole, ActorUPN, Comment, EntryRef, FromStatus, GORecordID, MetricCode, Notes, Timestamp, ToStatus, MetricName, MetricValue, SubmitterEmail, SubmitterName, ApproverEmail, ApproverName) are preserved by the Update item action (SharePoint UpdateItem only modifies specified fields).

---

## Test Cases (Planned)

| ID | Test | Input | Expected |
|---|---|---|---|
| TEST-01 | Approved path | Valid item, valid ApproverEmail | ApprovalStatus = Approved |
| TEST-02 | Rejected path | Valid item, valid ApproverEmail | ApprovalStatus = Rejected |
| TEST-03 | Missing ApproverEmail | Valid item, empty ApproverEmail | Error result |
| TEST-04 | Invalid SharePoint Item ID | Non-existent ID | Error result (item not found) |
| TEST-05 | SharePoint update verification | After TEST-01 or TEST-02 | Verify fields written correctly |
